# include <stdio.h>
# include <stdlib.h>
# include <math.h>



int main(int argc, char *argv[])
{
    int    k, iter;

    if (argc != 3) {
        fprintf (stdout, "wrong number of arguments!\n");
        exit (1);
    }
    k = atoi (argv[0]);
    iter = atoi (argv[1]);

    
   
}